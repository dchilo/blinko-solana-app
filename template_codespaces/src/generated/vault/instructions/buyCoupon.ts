/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const BUY_COUPON_DISCRIMINATOR = new Uint8Array([
  163, 156, 135, 250, 4, 243, 89, 98,
]);

export function getBuyCouponDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BUY_COUPON_DISCRIMINATOR);
}

export type BuyCouponInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TAccountMerchant extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      TAccountMerchant extends string
        ? ReadonlyAccount<TAccountMerchant>
        : TAccountMerchant,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BuyCouponInstructionData = { discriminator: ReadonlyUint8Array };

export type BuyCouponInstructionDataArgs = {};

export function getBuyCouponInstructionDataEncoder(): FixedSizeEncoder<BuyCouponInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: BUY_COUPON_DISCRIMINATOR }),
  );
}

export function getBuyCouponInstructionDataDecoder(): FixedSizeDecoder<BuyCouponInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getBuyCouponInstructionDataCodec(): FixedSizeCodec<
  BuyCouponInstructionDataArgs,
  BuyCouponInstructionData
> {
  return combineCodec(
    getBuyCouponInstructionDataEncoder(),
    getBuyCouponInstructionDataDecoder(),
  );
}

export type BuyCouponInput<
  TAccountBuyer extends string = string,
  TAccountOffer extends string = string,
  TAccountMerchant extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  buyer: TransactionSigner<TAccountBuyer>;
  offer: Address<TAccountOffer>;
  merchant: Address<TAccountMerchant>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getBuyCouponInstruction<
  TAccountBuyer extends string,
  TAccountOffer extends string,
  TAccountMerchant extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: BuyCouponInput<
    TAccountBuyer,
    TAccountOffer,
    TAccountMerchant,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BuyCouponInstruction<
  TProgramAddress,
  TAccountBuyer,
  TAccountOffer,
  TAccountMerchant,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    buyer: { value: input.buyer ?? null, isWritable: true },
    offer: { value: input.offer ?? null, isWritable: true },
    merchant: { value: input.merchant ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.buyer),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.merchant),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getBuyCouponInstructionDataEncoder().encode({}),
    programAddress,
  } as BuyCouponInstruction<
    TProgramAddress,
    TAccountBuyer,
    TAccountOffer,
    TAccountMerchant,
    TAccountSystemProgram
  >);
}

export type ParsedBuyCouponInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    buyer: TAccountMetas[0];
    offer: TAccountMetas[1];
    merchant: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: BuyCouponInstructionData;
};

export function parseBuyCouponInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBuyCouponInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      buyer: getNextAccount(),
      offer: getNextAccount(),
      merchant: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBuyCouponInstructionDataDecoder().decode(instruction.data),
  };
}
