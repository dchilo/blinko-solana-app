/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const REDEEM_COUPON_DISCRIMINATOR = new Uint8Array([
  66, 181, 163, 197, 244, 189, 153, 0,
]);

export function getRedeemCouponDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REDEEM_COUPON_DISCRIMINATOR,
  );
}

export type RedeemCouponInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMerchant extends string | AccountMeta<string> = string,
  TAccountOffer extends string | AccountMeta<string> = string,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountPlatform extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMerchant extends string
        ? WritableSignerAccount<TAccountMerchant> &
            AccountSignerMeta<TAccountMerchant>
        : TAccountMerchant,
      TAccountOffer extends string
        ? WritableAccount<TAccountOffer>
        : TAccountOffer,
      TAccountBuyer extends string
        ? WritableAccount<TAccountBuyer>
        : TAccountBuyer,
      TAccountPlatform extends string
        ? WritableAccount<TAccountPlatform>
        : TAccountPlatform,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RedeemCouponInstructionData = { discriminator: ReadonlyUint8Array };

export type RedeemCouponInstructionDataArgs = {};

export function getRedeemCouponInstructionDataEncoder(): FixedSizeEncoder<RedeemCouponInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REDEEM_COUPON_DISCRIMINATOR }),
  );
}

export function getRedeemCouponInstructionDataDecoder(): FixedSizeDecoder<RedeemCouponInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRedeemCouponInstructionDataCodec(): FixedSizeCodec<
  RedeemCouponInstructionDataArgs,
  RedeemCouponInstructionData
> {
  return combineCodec(
    getRedeemCouponInstructionDataEncoder(),
    getRedeemCouponInstructionDataDecoder(),
  );
}

export type RedeemCouponInput<
  TAccountMerchant extends string = string,
  TAccountOffer extends string = string,
  TAccountBuyer extends string = string,
  TAccountPlatform extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  merchant: TransactionSigner<TAccountMerchant>;
  offer: Address<TAccountOffer>;
  buyer: Address<TAccountBuyer>;
  platform: Address<TAccountPlatform>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRedeemCouponInstruction<
  TAccountMerchant extends string,
  TAccountOffer extends string,
  TAccountBuyer extends string,
  TAccountPlatform extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: RedeemCouponInput<
    TAccountMerchant,
    TAccountOffer,
    TAccountBuyer,
    TAccountPlatform,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RedeemCouponInstruction<
  TProgramAddress,
  TAccountMerchant,
  TAccountOffer,
  TAccountBuyer,
  TAccountPlatform,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    merchant: { value: input.merchant ?? null, isWritable: true },
    offer: { value: input.offer ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    platform: { value: input.platform ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.merchant),
      getAccountMeta(accounts.offer),
      getAccountMeta(accounts.buyer),
      getAccountMeta(accounts.platform),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRedeemCouponInstructionDataEncoder().encode({}),
    programAddress,
  } as RedeemCouponInstruction<
    TProgramAddress,
    TAccountMerchant,
    TAccountOffer,
    TAccountBuyer,
    TAccountPlatform,
    TAccountSystemProgram
  >);
}

export type ParsedRedeemCouponInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    merchant: TAccountMetas[0];
    offer: TAccountMetas[1];
    buyer: TAccountMetas[2];
    platform: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RedeemCouponInstructionData;
};

export function parseRedeemCouponInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRedeemCouponInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      merchant: getNextAccount(),
      offer: getNextAccount(),
      buyer: getNextAccount(),
      platform: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRedeemCouponInstructionDataDecoder().decode(instruction.data),
  };
}
