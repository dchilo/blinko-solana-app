/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const INITIALIZE_PRODUCT_DISCRIMINATOR = new Uint8Array([
  251, 245, 7, 123, 247, 50, 14, 2,
]);

export function getInitializeProductDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_PRODUCT_DISCRIMINATOR,
  );
}

export type InitializeProductInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountProductAccount extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountProductAccount extends string
        ? WritableAccount<TAccountProductAccount>
        : TAccountProductAccount,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeProductInstructionData = {
  discriminator: ReadonlyUint8Array;
  productId: string;
  name: string;
  description: string;
  metadataUri: string;
  price: bigint;
  paymentMint: Option<Address>;
  stock: number;
};

export type InitializeProductInstructionDataArgs = {
  productId: string;
  name: string;
  description: string;
  metadataUri: string;
  price: number | bigint;
  paymentMint: OptionOrNullable<Address>;
  stock: number;
};

export function getInitializeProductInstructionDataEncoder(): Encoder<InitializeProductInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["productId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["name", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["description", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["metadataUri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["price", getU64Encoder()],
      ["paymentMint", getOptionEncoder(getAddressEncoder())],
      ["stock", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_PRODUCT_DISCRIMINATOR }),
  );
}

export function getInitializeProductInstructionDataDecoder(): Decoder<InitializeProductInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["productId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["name", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["description", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["metadataUri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["price", getU64Decoder()],
    ["paymentMint", getOptionDecoder(getAddressDecoder())],
    ["stock", getU32Decoder()],
  ]);
}

export function getInitializeProductInstructionDataCodec(): Codec<
  InitializeProductInstructionDataArgs,
  InitializeProductInstructionData
> {
  return combineCodec(
    getInitializeProductInstructionDataEncoder(),
    getInitializeProductInstructionDataDecoder(),
  );
}

export type InitializeProductInput<
  TAccountAuthority extends string = string,
  TAccountProductAccount extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  productAccount: Address<TAccountProductAccount>;
  systemProgram?: Address<TAccountSystemProgram>;
  productId: InitializeProductInstructionDataArgs["productId"];
  name: InitializeProductInstructionDataArgs["name"];
  description: InitializeProductInstructionDataArgs["description"];
  metadataUri: InitializeProductInstructionDataArgs["metadataUri"];
  price: InitializeProductInstructionDataArgs["price"];
  paymentMint: InitializeProductInstructionDataArgs["paymentMint"];
  stock: InitializeProductInstructionDataArgs["stock"];
};

export function getInitializeProductInstruction<
  TAccountAuthority extends string,
  TAccountProductAccount extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: InitializeProductInput<
    TAccountAuthority,
    TAccountProductAccount,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeProductInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountProductAccount,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    productAccount: { value: input.productAccount ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.productAccount),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getInitializeProductInstructionDataEncoder().encode(
      args as InitializeProductInstructionDataArgs,
    ),
    programAddress,
  } as InitializeProductInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountProductAccount,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeProductInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    productAccount: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeProductInstructionData;
};

export function parseInitializeProductInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeProductInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      productAccount: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeProductInstructionDataDecoder().decode(instruction.data),
  };
}
